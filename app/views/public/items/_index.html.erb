<div>
  <div class="d-flex flex-wrap">
    <% items.each do |item| %>
      <div class="item card mr-3 d-flex flex-column mb-4">
        <%= link_to item_path(item.id) do %>
          <%= image_tag item.generate_variant_image(180, 120), class: 'm-1 img-fluid mb-1' %>
          <div class="">
          </div>
          <p class="item_name ml-1 text-dark"><%= item.name %></p>
          <span class="ml-1 text-dark">¥<%= item.with_tax_price.to_s(:delimited) %></span>
        <% end %>
      </div>
    <% end %>
  </div>
  <div class="d-flex justify-content-center text-center">
    <%= paginate items %>
  </div>
</div>